import './styles.css';

export const metadata = {
  title: 'Operator Workbench · Token Pulse',
  description: 'Outcome-first playbooks connected to Token Pulse and Growth Command evidence.',
  robots: { index: false, follow: false },
};

export default function OperatorLayout({ children }) {
  return children;
}
