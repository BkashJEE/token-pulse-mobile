{
  "name": "token-pulse-mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "check": "next build",
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage tests/*.test.js"
    ,"audit:secrets": "node scripts/secret-scan.cjs"
    ,"guard:push": "npm run audit:secrets && npm test && npm run build"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0",
    "next": "^16.2.11",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "overrides": {
    "postcss": "8.5.12",
    "sharp": "0.35.3"
  },
  "devDependencies": {
    "react-test-renderer": "19.2.7"
  }
}
